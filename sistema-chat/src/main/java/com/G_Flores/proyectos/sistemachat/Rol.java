package com.G_Flores.proyectos.sistemachat;

// Enum para definir los roles de usuario, Limitar valores: Una variable de tipo enum solo puede tomar uno de los valores predefinidos en la lista.
public enum Rol {
    EMPLEADO,
    ADMINISTRADOR
}